from cartography.models.core.nodes import ExtraNodeLabel

GIT_LAB_REPOSITORY = ExtraNodeLabel(
    label="GitLabRepository",
    description="A gitlab node participating in the shared GitLabRepository graph interface.",
)
