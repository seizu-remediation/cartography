from cartography.models.core.nodes import ExtraNodeLabel

ENVIRONMENT = ExtraNodeLabel(
    label="Environment",
    description="A workos node participating in the shared Environment graph interface.",
)
